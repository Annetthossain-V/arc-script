#include "parse_kw.h"
#include "parse.h"

bool pkw_generic(unsigned int *i, struct _lexer_token **lex_tokens, unsigned int *lex_len, struct _parsed_bytecode *bcode, size_t bcode_len) {
  return false;
}
